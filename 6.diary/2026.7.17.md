2026-07-17 开发日志

今天完成了什么：

完成了 motor_test.py 的第一版设计。

学会使用 assert 对 Motor 模块进行自动化测试。

将测试分为正常速度测试（test_speed）和边界测试（test_limit），理解了为什么要把输入值和期望值分开。

深入理解了 Python 中对象、属性和 . 运算符的含义，弄清了 motor.speed 表示的是“访问 motor 对象中的 speed 属性”，以及 self 的真正作用。

决定调整暑假学习计划：先完成 ESP32 小车 RobotLab V1.0，再系统学习 C 语言，之后进入 STM32。

今天遇到的问题：

一开始对 self 和 motor.speed 的关系理解不清，误以为类外也可以使用 self。

不理解为什么测试函数需要多个参数，以及什么时候应该把固定值改成参数。

怎么解决的：

通过分析对象、变量和属性之间的关系，理解了 . 是访问对象成员，而 self 只是类方法中的一个参数名。

理解了“凡是以后可能变化的东西，都应该考虑参数化”，因此将边界测试设计为 input_speed 和 expected_speed 两个参数，而不是写死固定值。

这次调试中，我更享受哪个环节：

待定