
学习内容：

1. 学习蜂鸣器(Buzzer)控制
2. 理解sleep_ms()与ticks_ms()区别
3. 编写Buzzer类
4. 将Button与Buzzer接入Controller
5. 分析Button边沿检测逻辑
6. 理解State(状态)与Event(事件)区别

收获：

以前只知道代码能运行。
今天开始尝试推演程序执行过程。

重点理解：

状态(State)：
mode == 2

事件(Event)：
button.is_pressed()

发现：

if mode == 2:
    buzzer.beep()

属于状态触发。

会不断执行。

而：

if button.is_pressed():
    buzzer.beep()

属于事件触发。

只会执行一次。

问题：

Controller中mode==2时蜂鸣器逻辑仍需实际接线验证。

下一步：

完成Button+Buzzer硬件测试。
学习HC-SR04超声波模块。