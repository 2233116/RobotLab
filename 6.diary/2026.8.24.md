今天记录：

学习节点

Encoder 类第一版职责与数据结构设计

已完成

确定 Encoder 当前的属性方向：

ppr
pulse_count
start_time
current_speed

确定方法方向：

on_pulse()
update()
get_current_speed()

确定数据流：

编码器产生脉冲
↓
ESP32 中断
↓
encoder.on_pulse()
↓
pulse_count += 1

主循环 / 调度层
↓
encoder.update()
↓
Encoder 自己判断测速周期
↓
计算 RPM
↓
更新 current_speed
↓
pulse_count 重置
↓
开始下一测速窗口

Controller
↓
encoder.get_current_speed()
↓
得到最近一次有效速度

今天还明确了：

get_current_speed() 只负责读取结果，不重新测速

update() 不阻塞等待

主循环只负责给 Encoder 更新机会，不负责知道 Encoder 的 200ms 测速周期

Encoder 自己管理测速周期

中断函数不直接操作 pulse_count，而是调用 encoder.on_pulse()
