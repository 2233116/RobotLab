日期： 2026-07-07

完成了什么：

深入理解了面向对象中的 Class、Object、变量名和 self 的关系。

能够解释 "谁调用方法，self 就是谁" 的原理。

理解了为什么每个对象拥有自己独立的属性，不同对象之间互不影响。

学习了 PWM 对象的职责，理解了 PWM 对象需要保存引脚、频率和占空比等状态。

理解了 Motor 类中保存 speed、direction、is_running 等状态的意义。

理解了 Controller 负责发送命令，Motor 负责维护自身状态和控制硬件，两者职责分离。

初步理解了停止电机时，软件状态（speed）与硬件状态（AIN1、AIN2、PWM）可以采用不同的设计策略，应根据实际需求决定。


遇到的问题：

一开始混淆了 Class、Object 和变量名的概念。

不理解 self 为什么能够代表不同对象。

不理解停止时为什么 PWM 可以保持原来的占空比。


怎么解决的：

通过汽车设计图、对象实例和变量名的类比，理解了 Class 与 Object 的关系。

通过分析 left_motor.forward() 与 right_motor.forward() 的调用过程，理解了 "谁调用方法，self 就是谁"。

从"方向控制（AIN1、AIN2）"和"速度控制（PWM）"两个层次分析了电机驱动过程，理解了 H 桥与 PWM 各自的职责。


这次调试中，我更享受哪个环节：
依旧期待硬件