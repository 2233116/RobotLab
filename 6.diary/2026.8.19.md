今日学习主题： DifferentialDrive 接入两个 Motor，完成完整数据流测试

今日目标：

将已经完成的 Controller、DifferentialDrive 和两个 Motor 连接起来，验证从控制目标到左右电机执行的完整数据流。

今日完成内容：

1. 继续完善 DifferentialDrive。


2. 确认 DifferentialDrive 当前负责根据 output 和 turn 计算左右轮速度。


3. 明确采用 B 方案：

DifferentialDrive 不直接持有 Motor 对象。

DifferentialDrive 负责计算 left_speed 和 right_speed。

外部程序负责将计算结果传递给左右两个 Motor。



4. 完成 Controller → DifferentialDrive 的数据流测试。


5. 验证目标速度为 50、转向量为 20 时：

左轮速度为 70

右轮速度为 30



6. 在 differential_drive_test 中加入 DifferentialDrive → Motor × 2 的完整数据流测试。


7. PC 端运行测试时，前面的 DifferentialDrive 测试全部通过。


8. 运行到 Motor 时发现 PC Python 环境不存在 machine 模块。


9. 理解了 PC Python 与 ESP32 MicroPython 的运行环境区别：

Motor 依赖 ESP32 MicroPython 提供的 machine 模块。

因此不能直接在普通 PC Python 环境中运行这个 Motor 实机模块。



10. 确认不修改 Motor，也不为了 PC 测试去安装或伪造 machine。


11. 实机测试推迟到下一次学习。



今日遇到的问题：

PC Python 执行 Motor 模块时无法找到 machine。

问题原因：

machine 是 MicroPython 面向硬件平台提供的模块，PC 上的普通 Python 环境并不提供它。

今日得到的理解：

RobotLab 的测试开始出现两个不同层次：

逻辑测试： 可以在 PC Python 环境运行。

硬件实机测试： 需要在 ESP32 的 MicroPython 环境运行。


今日状态：

Controller → DifferentialDrive：完成

DifferentialDrive 计算：完成

DifferentialDrive → 两个 Motor 的代码连接：完成

ESP32 实机验证：未完成


下一步：

在 ESP32 / MicroPython 环境中进行 DifferentialDrive → Motor × 2 完整实机测试。