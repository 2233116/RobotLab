DifferentialDrive 测试与 RobotLab 开发环境配置

今日完成

1. DifferentialDrive 测试

完成右转场景的逻辑测试：

Controller
    ↓ output = 50
DifferentialDrive
    ↓ turn = 20
left_speed = 70
right_speed = 30

测试验证通过。

目前暂时只测试右转，直行和左转后续再补。

2. VS Code / RobotLab 测试环境

解决了 VS Code 测试时无法找到 3.modules 中 Python 模块的问题。

确定长期采用：

> 方案 B：通过 Python 模块搜索路径让电脑端找到 3.modules，不修改核心模块的 import 方式。



最终实现：

VS Code
    ↓
PYTHONPATH
    ↓
3.modules/
    ↓
controller.py / differential_drive.py / motor.py

并实现了直接点击 VS Code ▶ 就能运行测试。

3. 学习认知

了解到：

PYTHONPATH 是 Python 的模块搜索路径。

settings.json 可以配置 VS Code 的开发环境。

这种开发环境配置目前只需要做到“会用、知道解决什么问题”，不需要现在系统学习。

后续在 Python 基础 → 工程化基础阶段，再系统学习 import / module / package / PYTHONPATH / .env / 虚拟环境 / Git/GitHub / 测试 等内容。