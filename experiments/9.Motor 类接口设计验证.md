实验目的

在未接入真实电机的情况下，完成 Motor 类的软件接口设计。

验证对象职责划分是否合理。


实验内容

设计 forward(speed)、backward(speed) 接口。

分析 Pin 与 PWM 在 Motor 中的职责分配。

验证属性与参数的使用方式是否合理。


实验结论

AIN1、AIN2 使用 Pin。

PWMA 使用 PWM。

Motor 保存自身状态（speed、direction、is_running）。

Controller 通过参数下发控制命令，Motor 更新自身属性。
