新增实验记录（理论验证）

实验名称：Motor 类代码结构设计验证

实验目的： 验证 Motor 类各接口之间的调用关系是否合理。

验证内容：

forward()、backward() 调用 set_speed()。

stop() 调用 set_speed(0)。

set_speed() 负责统一更新速度状态和 PWM 输出。

软件状态与硬件输出保持一致。


实验结果： 完成理论设计验证，准备后续在 ESP32 实物上进行单电机测试。
